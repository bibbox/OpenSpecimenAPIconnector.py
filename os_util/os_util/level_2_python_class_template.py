import requests
import json
import pickle
import numpy as np
import json
import requests
import uuid
import faker
from faker import Factory
import names
import random
import time
import datetime
import pandas as pd
from datetime import date
from datetime import datetime
from dateutil.relativedelta import relativedelta
from request_factory import RequestFactory
from jsons import Json_factory
from datetime import date
from datetime import datetime
from dateutil.relativedelta import relativedelta
import zipfile
import tempfile


# additional imports

# import vvarious base classes you need here


# use inheritace to incooperate other util methods u might need
# e.g: if u need a complete other util class in this one do:
# class level_2_class_template(inherited_class) to get all the methods
# dont forget to use initialize the parameters the superclass(or the inherited class) might needs

class level_2_class_template():

    # also pass the superclasses parameters here
    def __init__(self):
        # super(Y, self).__init__(123) # initialize the parameters of the superclass
        pass

    def util_method_entities_operation(self):
        # do smt
        pass

